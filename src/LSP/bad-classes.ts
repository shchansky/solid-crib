// ❌ ПЛОХО: Нарушение LSP - классы-наследники не могут заменить базовый класс
//
// 📋 НАРУШЕНИЕ ОСНОВНОЙ ИДЕИ LSP:
// 1. ❌ НАРУШЕНИЕ КОНТРАКТА - классы-наследники не выполняют обещания базового класса (сужают постусловия, меняют формат )
// 2. ❌ НЕОЖИДАННОЕ ПОВЕДЕНИЕ - код может сломаться при замене базового класса на класс-наследник
// 3. ❌ НАРУШЕНИЕ ПРИНЦИПА ПОДСТАНОВКИ - классы-наследники не являются истинными подтипами
// 4. ❌ СЛОЖНОСТЬ ТЕСТИРОВАНИЯ - нужно тестировать каждый подкласс отдельно
// 5. ❌ НАРУШЕНИЕ ПОЛИМОРФИЗМА - код должен знать конкретный тип объекта
//
// 🚨 ПРОБЛЕМЫ:
// 1. Нарушение контракта - классы-наследники не выполняют обещания базового класса
// 2. Неожиданное поведение - код может сломаться при замене базового класса на класс-наследник
// 3. Нарушение принципа подстановки - классы-наследники не являются истинными подтипами
// 4. Сложность тестирования - нужно тестировать каждый класс-наследник отдельно
// 5. Нарушение полиморфизма - код должен знать конкретный тип объекта

// ❌ НАРУШЕНИЕ LSP: Базовый класс с нечетким контрактом
// 💡 ЧТО НЕ ТАК: Базовый класс предполагает прямоугольную форму
// 🔧 РЕШЕНИЕ: Создать интерфейс Shape с общими методами


type InfoType=`Shape: ${string}x${string}`

class Shape {
    protected width: number;
    protected height: number;

    constructor(width: number, height: number) {
        this.width = width;
        this.height = height;
    }

    // ✅ Базовый метод - обещает вернуть площадь
    getArea(): number {
        return this.width * this.height;
    }

    // ✅ Базовый метод - обещает вернуть периметр
    getPerimeter(): number {
        return 2 * (this.width + this.height);
    }

    // ✅ Базовый метод - обещает вернуть информацию о фигуре
    getInfo(): InfoType {
        return `Shape: ${this.width}x${this.height}`;
    }
}

// ❌ НАРУШЕНИЕ LSP: Круг не может заменить Shape
// 💡 ЧТО НЕ ТАК: Круг имеет только радиус, а не ширину и высоту
// 🎯 РЕЗУЛЬТАТ: Нарушение контракта базового класса
class Circle extends Shape {
    private radius: number;

    constructor(radius: number) {
        // ❌ ПРОБЛЕМА: Передаем радиус как width и height, что нелогично
        // 💡 ЧТО НЕ ТАК: Круг не имеет ширины и высоты в традиционном смысле
        // 🎯 РЕШЕНИЕ: Circle не должен наследоваться от Shape
        super(radius * 2, radius * 2);
        this.radius = radius;
    }

    // ✅ ПРАВИЛЬНО: Переопределяем getArea() с правильным поведением для круга
    getArea(): number {
        return Math.PI * this.radius * this.radius; 
    }

    // ❌ НАРУШЕНИЕ LSP: Переопределяем getPerimeter() с неправильным поведением
    // 💡 ЧТО НЕ ТАК: Возвращаем диаметр вместо периметра (длины окружности)
    // 🎯 РЕЗУЛЬТАТ: Нарушение контракта - метод не выполняет обещание
    getPerimeter(): number {
        return 2 * this.radius; // ❌ Это диаметр, не периметр!
    }

    // ❌ НАРУШЕНИЕ LSP: Переопределяем getInfo() с другим форматом
    // 💡 ЧТО НЕ ТАК: Другой формат информации нарушает ожидания
    // 🎯 РЕЗУЛЬТАТ: Неожиданное поведение для клиентов
    // 💥 ОШИБКА КОМПИЛЯЦИИ: TypeScript показывает нарушение контракта!
    getInfo(): InfoType {
        // ❌ Этот код НЕ КОМПИЛИРУЕТСЯ без принудительного приведения!
        // return `Circle: radius=${this.radius}`; // ❌ Type error!
        
        // ❌ Принудительное приведение скрывает проблему
        return `Circle: radius=${this.radius}` as InfoType;
    }
}

// ❌ НАРУШЕНИЕ LSP: Треугольник не может заменить Shape
// 💡 ЧТО НЕ ТАК: Треугольник имеет три стороны, а не ширину и высоту
// 🎯 РЕЗУЛЬТАТ: Нарушение контракта базового класса
class Triangle extends Shape {
    private side1: number;
    private side2: number;
    private side3: number;

    constructor(side1: number, side2: number, side3: number) {
        // ❌ ПРОБЛЕМА: Передаем стороны как width и height, что нелогично
        // 💡 ПРОБЛЕМА: Треугольник не имеет ширины и высоты в традиционном смысле
        // 🎯 РЕШЕНИЕ: Triangle не должен наследоваться от Shape
        super(side1, side2);
        this.side1 = side1;
        this.side2 = side2;
        this.side3 = side3;
    }

    // ✅ ПРАВИЛЬНО: Переопределяем getArea() с правильным поведением для треугольника
    getArea(): number {
        // Формула Герона для треугольника
        const s = (this.side1 + this.side2 + this.side3) / 2;
        return Math.sqrt(s * (s - this.side1) * (s - this.side2) * (s - this.side3));
    }

    // ✅ ПРАВИЛЬНО: Переопределяем getPerimeter() с правильным поведением для треугольника
    getPerimeter(): number {
        return this.side1 + this.side2 + this.side3; // Правильно для треугольника
    }

    // ❌ НАРУШЕНИЕ LSP: Переопределяем getInfo() с другим форматом
    // 💡 ЧТО НЕ ТАК: Другой формат информации нарушает ожидания
    // 🎯 РЕЗУЛЬТАТ: Неожиданное поведение для клиентов
    getInfo(): InfoType {
        return `Triangle: sides=${this.side1},${this.side2},${this.side3}` as InfoType; // Другой формат информации
    }
}

// ❌ НАРУШЕНИЕ LSP: Функция, которая ожидает Shape, но может сломаться с подклассами
// 💡 ЧТО НЕ ТАК: Функция может получить неожиданное поведение от подклассов
// 🎯 РЕЗУЛЬТАТ: Нарушение принципа подстановки Лисков
function processShape(shape: Shape): {
    info: string;
    area: number;
    perimeter: number;
} {
    return {
        info: shape.getInfo(),
        area: shape.getArea(),
        perimeter: shape.getPerimeter(),
    };
}

// ❌ ДЕМОНСТРАЦИЯ ПРОБЛЕМ: Неожиданное поведение
// 💡 ЧТО НЕ ТАК: Подклассы не могут заменить базовый класс
// 🎯 РЕЗУЛЬТАТ: Нарушение принципа подстановки
const lspRectangle = new Shape(4, 6);
const lspCircle = new Circle(5);
const lspTriangle = new Triangle(3, 4, 5);

processShape(lspRectangle); // ✅ Работает как ожидается

processShape(lspCircle); // ❌ Будет работать, но поведение неожиданное
// 💡 ЧТО НЕ ТАК: getPerimeter() вернет диаметр вместо периметра

processShape(lspTriangle); // ❌ Будет работать, но поведение неожиданное
// 💡 ЧТО НЕ ТАК: getInfo() вернет другой формат информации

// ❗ Пояснение: нарушение контракта (LSP)
// - Сужение постусловий: метод getPerimeter() в Circle возвращает диаметр вместо длины окружности
// - Изменение формата: метод getInfo() в Circle/Triangle возвращает иной формат, чем базовый контракт
// - Следствие: клиенты, рассчитывающие на контракт Shape, получают неожиданные значения/форматы

// 🧪 Юнит‑тест (псевдо) — как клиенты ломаются из‑за нарушения контракта
// function assertLsp(shape: Shape) {
//     const info = shape.getInfo();
//     // Клиент ожидает формат базового контракта
//     const matches = /^Shape: \d+x\d+$/.test(info);
//     if (!matches) throw new Error('Нарушение контракта формата getInfo');
//     const perimeter = shape.getPerimeter();
//     if (perimeter <= 0) throw new Error('Нарушение постусловий getPerimeter');
// }
// assertLsp(lspRectangle); // ✅
// // Следующие вызовы демонстрируют сбой контракта:
// // assertLsp(lspCircle);   // ❌ бросит ошибку из‑за формата/значения
// // assertLsp(lspTriangle); // ❌ бросит ошибку из‑за формата

export {}