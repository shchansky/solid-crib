# 🔄 Dependency Inversion Principle (DIP)

## 🎯 Что это такое?

**Принцип инверсии зависимостей** - высокоуровневые программные сущности не должны зависеть от низкоуровневых программных сущностей. Оба должны зависеть от абстракций. Абстракции не должны зависеть от деталей. Детали должны зависеть от абстракций.

> "Зависимости должны направляться к абстракциям, а не к конкретным реализациям" - Роберт Мартин

## 🔄 ОСНОВНАЯ ИДЕЯ:

### **1. ВЫСОКОУРОВНЕВЫЕ ПРОГРАММНЫЕ СУЩНОСТИ НЕ ДОЛЖНЫ ЗАВИСЕТЬ ОТ НИЗКОУРОВНЕВЫХ**

**❌ ПЛОХО:**
```typescript
// Высокоуровневая программная сущность зависит от низкоуровневых
class ShapeCalculator {
    calculateCircle(radius: number) {
        const circle = new Circle(radius); // ❌ Зависимость от конкретного класса
        return circle.getArea();
    }
}
```

**✅ ХОРОШО:**
```typescript
// Высокоуровневая программная сущность зависит от абстракции
class ShapeCalculator {
    calculateShape(shape: Shape) { // ✅ Зависимость от интерфейса
        return shape.getArea();
    }
}
```

### **2. ОБА ТИПА ДОЛЖНЫ ЗАВИСЕТЬ ОТ АБСТРАКЦИЙ**

**❌ ПЛОХО:**
```typescript
// Высокоуровневая зависит от конкретных, низкоуровневая не зависит от абстракций
class Calculator {
    private fileStorage = new FileStorage(); // ❌ Конкретная зависимость
}

class Circle {
    // ❌ Нет общего интерфейса
}
```

**✅ ХОРОШО:**
```typescript
// Оба зависят от абстракций
class Calculator {
    constructor(private storage: Storage) {} // ✅ Абстракция
}

class Circle implements Shape { // ✅ Реализует абстракцию
    getArea() { return Math.PI * this.radius * this.radius; }
}
```

### **3. АБСТРАКЦИИ НЕ ДОЛЖНЫ ЗАВИСЕТЬ ОТ ДЕТАЛЕЙ**

**❌ ПЛОХО:**
```typescript
// Абстракция знает о деталях
interface Shape {
    getArea(): number;
    getCircleRadius(): number; // ❌ Знает о конкретной реализации
}
```

**✅ ХОРОШО:**
```typescript
// Абстракция не знает о деталях
interface Shape {
    getArea(): number;      // ✅ Общие методы для всех фигур
    getPerimeter(): number; // ✅ Общие методы для всех фигур
}
```

### **4. ДЕТАЛИ ДОЛЖНЫ ЗАВИСЕТЬ ОТ АБСТРАКЦИЙ**

**❌ ПЛОХО:**
```typescript
// Детали не зависят от абстракций
class Circle {
    getArea() { return Math.PI * this.radius * this.radius; }
    // ❌ Нет общего интерфейса
}
```

**✅ ХОРОШО:**
```typescript
// Детали реализуют абстракции
class Circle implements Shape {
    getArea() { return Math.PI * this.radius * this.radius; }
    getPerimeter() { return 2 * Math.PI * this.radius; }
    // ✅ Реализует интерфейс Shape
}
```

## 🚀 Ключевые принципы:

### 1. **Инверсия зависимостей**
- Высокоуровневые программные сущности зависят от абстракций
- Низкоуровневые программные сущности реализуют абстракции
- Зависимости направлены к абстракциям, а не к конкретным реализациям

### 2. **Абстракции**
- Интерфейсы и типы определяют контракты
- Абстракции не зависят от деталей реализации
- Абстракции стабильны и редко изменяются

### 3. **Инъекция зависимостей**
- Зависимости передаются извне
- Классы не создают свои зависимости внутри себя
- Легкая замена зависимостей для тестирования

### 4. **Слабая связанность**
- Программные сущности слабо связаны между собой
- Изменения в одной программной сущности не влияют на другие
- Легкость модификации и расширения

### 5. **Полиморфизм**
- Использование полиморфизма для работы с абстракциями
- Один интерфейс, множество реализаций
- Гибкость в выборе конкретных реализаций



## 🔍 Как определить нарушения DIP:

### Вопросы для проверки:

1. **Создает ли класс свои зависимости внутри себя?**
   - Если да - нарушение DIP

2. **Зависит ли высокоуровневая программная сущность от конкретных реализаций?**
   - Если да - нарушение DIP

3. **Использует ли код проверки типов (instanceof)?**
   - Если да - возможное нарушение DIP

4. **Можно ли легко заменить зависимости для тестирования?**
   - Если нет - нарушение DIP

5. **Создает ли функция объекты внутри себя?**
   - Если да - нарушение DIP

6. **Зависит ли абстракция от деталей реализации?**
   - Если да - нарушение DIP


## 🎯 Итог:

DIP - это принцип, который обеспечивает:
- Слабую связанность между модулями
- Легкость тестирования и замены зависимостей
- Гибкость и расширяемость архитектуры
- Устойчивость к изменениям

*Зависимости должны направляться к абстракциям, а не к конкретным реализациям!** 