// ❌ ПЛОХО: Нарушение DIP - классы зависят от конкретных реализаций, а не от абстракций
//
// 🔄 НАРУШЕНИЕ ОСНОВНОЙ ИДЕИ DIP:
// 1. ❌ ВЫСОКОУРОВНЕВЫЕ МОДУЛИ ЗАВИСЯТ ОТ НИЗКОУРОВНЕВЫХ
//    - BadShapeCalculator (высокоуровневый) зависит от Circle, Rectangle, Triangle (низкоуровневые)
//    - BadShapeCalculator создает конкретные объекты: new Circle(), new Rectangle(), new Triangle()
//
// 2. ❌ НЕ ОБА ТИПА ЗАВИСЯТ ОТ АБСТРАКЦИЙ
//    - Высокоуровневые модули: BadShapeCalculator зависит от конкретных классов
//    - Низкоуровневые модули: Circle, Rectangle, Triangle НЕ реализуют общий интерфейс
//
// 3. ❌ АБСТРАКЦИИ ЗАВИСЯТ ОТ ДЕТАЛЕЙ (их просто нет!)
//    - Нет интерфейсов Shape, Storage, Display
//    - Все зависимости направлены к конкретным классам
//
// 4. ❌ ДЕТАЛИ НЕ ЗАВИСЯТ ОТ АБСТРАКЦИЙ
//    - Circle, Rectangle, Triangle НЕ реализуют общий интерфейс
//    - Каждый класс живет своей жизнью без общего контракта
//
// ПРОБЛЕМЫ:
// 1. Жесткая связанность - классы зависят от конкретных классов
// 2. Нарушение принципа инверсии зависимостей - зависимости направлены к конкретным реализациям
// 3. Сложность тестирования - нельзя легко заменить зависимости
// 4. Нарушение принципа открытости/закрытости - изменения в зависимостях влияют на основной класс
// 5. Сложность расширения - добавление новых типов требует изменения существующего кода

// ❌ НАРУШЕНИЕ DIP: Конкретные классы для разных типов фигур
// 🔄 ПРОБЛЕМА: Нет общего интерфейса - детали не зависят от абстракций
// 💡 ЧТО НЕ ТАК: Каждый класс реализует свои методы без общего контракта
// 🎯 КАК ИСПРАВИТЬ: Создать интерфейс Shape и заставить все классы его реализовывать

class Circle {
    constructor(private radius: number) {}

    getArea(): number {
        return Math.PI * this.radius * this.radius;
    }

    getPerimeter(): number {
        return 2 * Math.PI * this.radius;
    }

    getInfo(): string {
        return `Circle: radius=${this.radius}`;
    }
}

class Rectangle {
    constructor(private width: number, private height: number) {}

    getArea(): number {
        return this.width * this.height;
    }

    getPerimeter(): number {
        return 2 * (this.width + this.height);
    }
}

// ❌ НАРУШЕНИЕ DIP: Интерфейс данных зависит от конкретных реализаций
// 🔄 ПРОБЛЕМА: Интерфейс знает о конкретных типах фигур
// 💡 ЧТО НЕ ТАК: Интерфейс привязан к конкретным классам Circle и Rectangle
// 🎯 КАК ИСПРАВИТЬ: Создать общий интерфейс для всех фигур
interface ShapeData {
    circledata: { radius: number };
    rectangledata: { width: number; height: number };
}

// ❌ НАРУШЕНИЕ DIP: Класс зависит от конкретных реализаций
// 🔄 ПРОБЛЕМА: Высокоуровневый модуль зависит от низкоуровневых
// 💡 ЧТО НЕ ТАК: 
//    - Класс создает конкретные объекты внутри себя
//    - Зависит от конкретных типов Circle и Rectangle
//    - Нельзя легко заменить реализации для тестирования
// 🎯 КАК ИСПРАВИТЬ: 
//    - Создать интерфейс Shape
//    - Передавать зависимости через конструктор
//    - Работать с абстракциями, а не с конкретными типами
class ShapeCalculator {
    // ❌ ПРОБЛЕМА: Зависимость от конкретных классов
    // 🔄 НАРУШЕНИЕ: Высокоуровневый модуль зависит от низкоуровневых
    private circle: Circle;
    private rectangle: Rectangle;

    constructor({circledata : {radius}, rectangledata : {width, height}} : ShapeData) {
        // ❌ НАРУШЕНИЕ DIP: Создание конкретных зависимостей внутри класса
        // 🔄 НАРУШЕНИЕ: Высокоуровневый модуль создает низкоуровневые модули и зависит от них
        // 💡 ПРОБЛЕМА: Класс сам создает свои зависимости - это нарушает DIP
        // 🎯 РЕШЕНИЕ: Зависимости должны передаваться извне через конструктор (паттерн адаптер)
        this.circle = new Circle(radius);
        this.rectangle = new Rectangle(width, height);
    }

    // ❌ ПРОБЛЕМА: Методы работают с конкретными типами
    // 💡 ЧТО НЕ ТАК: Каждый метод знает о конкретном типе фигуры
    // 🎯 КАК ИСПРАВИТЬ: Создать общий метод, работающий с интерфейсом Shape
    calculateCircleArea(): number {
        return this.circle.getArea();
    }

    calculateRectangleArea(): number {
        return this.rectangle.getArea();
    }

    // ❌ ПРОБЛЕМА: Невозможно добавить новую фигуру без изменения класса
    // 💡 ЧТО НЕ ТАК: Для добавления,например,треугольника Triangle нужно изменить этот класс
    // 🎯 КАК ИСПРАВИТЬ: Работать с массивом объектов Shape
    // calculateTriangleArea(): number {
    //     // Пришлось бы добавить новый метод и новое поле
    // }
}

// ❌ ДЕМОНСТРАЦИЯ ПРОБЛЕМ: Сложность тестирования
// 💡 ПРОБЛЕМА: Нельзя легко заменить Circle на мок для тестирования
// 🎯 РЕШЕНИЕ: Использовать интерфейсы и инъекцию зависимостей
function demonstrateTestingProblems() {
    const calculator = new ShapeCalculator({
        circledata: { radius: 5 },
        rectangledata: { width: 4, height: 6 }
    });
    
    // ❌ ПРОБЛЕМА: Нельзя протестировать с мок-объектами
    // const mockCalculator = new BadShapeCalculator({
    //     // Невозможно передать мок-объекты вместо реальных Circle и Rectangle
    // });
}

export {}

