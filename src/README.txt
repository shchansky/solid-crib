Добавить раздел “Мини-алгоритм диагностики в проде” по каждому принципу:
SRP: “Файл тяжёлый, смешаны уровни абстракции, название не описывает одну ответственность”.
OCP: “switch/if по типам”, “новая фича трогает старый код”.
LSP: “подкласс меняет формат/исключения/инварианты”.
ISP: “пустые реализации/return 0/null”, “интерфейс слишком универсален”.
DIP: “new внутри высокоуровневого модуля”, “instanceof/конкретные типы в логике”.


SRP: “Одна причина для изменения” → валидация, вычисления, координация — отдельно.
OCP: не перечисляй типы — дай интерфейс/тип и новые реализации.
LSP: подтип не меняет контракт и поведение, только детали реализации.
ISP: лучше 3 маленьких интерфейса, чем 1 толстый.
DIP: верхний слой знает только про интерфейсы, не про классы; зависимости приходят снаружи.