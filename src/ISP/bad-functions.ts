// ❌ ПЛОХО: Нарушение ISP - функции работают с толстыми типами, содержащими ненужные методы
//
// 🔀 НАРУШЕНИЕ ОСНОВНОЙ ИДЕИ ISP:
// 1. ❌ КЛИЕНТЫ ЗАВИСЯТ ОТ МЕТОДОВ, КОТОРЫЕ НЕ ИСПОЛЬЗУЮТ
//    - createCircle вынужден реализовывать getDiagonal(), который ему не нужен
//    - createRectangle вынужден реализовывать getDiametr(), который ему не нужен
//
// 2. ❌ ТОЛСТЫЙ УНИВЕРСАЛЬНЫЙ ТИП
//    - Один тип пытается покрыть все возможные методы
//    - Нарушение принципа единственной ответственности
//
// 3. ❌ ПРИНУДИТЕЛЬНАЯ РЕАЛИЗАЦИЯ НЕНУЖНЫХ МЕТОДОВ
//    - Функции должны возвращать объекты с методами, которые не имеют смысла
//    - Возвращают бессмысленные значения (0, null, undefined)
//
// ПРОБЛЕМЫ:
// 1. Толстые типы - содержат слишком много методов
// 2. Принудительная зависимость - функции зависят от ненужных методов
// 3. Нарушение принципа единственной ответственности - функции знают о слишком многом
// 4. Сложность тестирования - нужно создавать объекты со всеми методами
// 5. Нарушение принципа подстановки - функции могут случайно вызвать ненужные методы

// ❌ НАРУШЕНИЕ ISP: Толстый тип для фигуры, который содержит все возможные методы
// 💡 ЧТО НЕ ТАК: Тип содержит методы, которые не нужны всем реализующим объектам
// 🎯 КАК ИСПРАВИТЬ: Разделить на несколько специализированных типов
type Shape = {
    // ✅ Основные методы для всех фигур - это нормально
    getArea(): number;
    getPerimeter(): number;
    getInfo(): string;
    
    // ❌ ПРОБЛЕМА: Метод подходит только для круга
    // 💡 ЧТО НЕ ТАК: Rectangle не может иметь диаметр
    // 🎯 РЕШЕНИЕ: Вынести в отдельный тип CircleShape
    getDiametr(): number;

    // ❌ ПРОБЛЕМА: Метод подходит только для прямоугольника
    // 💡 ЧТО НЕ ТАК: Circle не может иметь диагональ
    // 🎯 РЕШЕНИЕ: Вынести в отдельный тип RectangleShape
    getDiagonal(): number;
};

// ❌ НАРУШЕНИЕ ISP: Фабричные функции создают объекты с ненужными методами
// 💡 ПРОБЛЕМА: Circle должен реализовывать методы, которые ему не нужны
// 🎯 РЕЗУЛЬТАТ: Возвращает бессмысленные значения
function createCircle(radius: number): Shape {
    return {
        getArea: () => Math.PI * radius * radius,
        getPerimeter: () => 2 * Math.PI * radius,
        getInfo: () => `Circle: radius=${radius}`,
        getDiametr: () => radius * 2, 
        // ❌ НАРУШЕНИЕ ISP: Круг должен реализовывать методы, которые ему не нужны
        // 💡 ПРОБЛЕМА: Возвращает бессмысленное значение
        // 🎯 РЕЗУЛЬТАТ: Нарушение принципа подстановки Лисков
        getDiagonal: () => 0, // Пустая реализация
    };
}

// ❌ НАРУШЕНИЕ ISP: Фабричные функции создают объекты с ненужными методами
// 💡 ПРОБЛЕМА: Rectangle должен реализовывать методы, которые ему не нужны
// 🎯 РЕЗУЛЬТАТ: Возвращает бессмысленные значения
function createRectangle(width: number, height: number): Shape {
    return {
        getArea: () => width * height,
        getPerimeter: () => 2 * (width + height),
        getInfo: () => `Rectangle: ${width}x${height}`,
        // ❌ НАРУШЕНИЕ ISP: Прямоугольник должен реализовывать методы, которые ему не нужны
        // 💡 ПРОБЛЕМА: Возвращает бессмысленное значение
        // 🎯 РЕЗУЛЬТАТ: Нарушение принципа подстановки Лисков
        getDiametr: () => 0, 
        getDiagonal: () => Math.sqrt(width * width + height * height),
    };
}

// ❌ НАРУШЕНИЕ ISP: Фабрика создает объекты с ненужными полями
// 💡 ПРОБЛЕМА: Каждый объект содержит поля, которые ему не нужны
// 🎯 РЕЗУЛЬТАТ: Избыточность данных и путаница
const createShapeFactory = {
    // Фабричная функция для круга
    circle: (radius: number) => {
        const circle = createCircle(radius);
        return {
            area: circle.getArea(),
            perimeter: circle.getPerimeter(),
            info: circle.getInfo(),
            diameter: circle.getDiametr(),
            diagonal: circle.getDiagonal(), // ❌ Ненужное поле для круга
        };
    },
    
    // Фабричная функция для прямоугольника
    rectangle: (width: number, height: number) => {
        const rectangle = createRectangle(width, height);
        return {
            area: rectangle.getArea(),
            perimeter: rectangle.getPerimeter(),
            info: rectangle.getInfo(),
            diameter: rectangle.getDiametr(), // ❌ Ненужное поле для прямоугольника
            diagonal: rectangle.getDiagonal(),
        };
    }
};

// ❌ ДЕМОНСТРАЦИЯ ПРОБЛЕМ: Избыточность данных
// 💡 ПРОБЛЕМА: Объекты содержат ненужные поля
// 🎯 РЕЗУЛЬТАТ: Путаница и избыточность
const badCircleData = createShapeFactory.circle(10);
const badRectangleData = createShapeFactory.rectangle(4, 6);

// ❌ ПРОБЛЕМА: Нельзя легко определить, какие поля действительно нужны
// console.log(badCircleData.diagonal); // 0 - бессмысленное значение
// console.log(badRectangleData.diameter); // 0 - бессмысленное значение

// ❌ ДЕМОНСТРАЦИЯ ПРОБЛЕМ: Сложность тестирования
// 💡 ПРОБЛЕМА: Нужно создавать объекты со всеми методами
// 🎯 РЕШЕНИЕ: Использовать специализированные типы
function demonstrateTestingProblems() {
    // ❌ ПРОБЛЕМА: Нужно реализовывать все методы для тестирования
    // const mockShape: Shape = {
    //     getArea: () => 10,
    //     getPerimeter: () => 20,
    //     getInfo: () => 'Mock',
    //     getDiametr: () => 0, // Ненужный метод
    //     getDiagonal: () => 0, // Ненужный метод
    // };
}

export {}
